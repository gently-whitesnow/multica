---
title: Service principals API
description: Workspace-scoped machine identities for external integrations.
---

Service principals authenticate external integrations without impersonating a
Multica member or agent. Management endpoints require a human workspace owner
or admin. Send human authentication normally and use the workspace UUID in the
path.

## Management endpoints

| Method | Path | Result |
| --- | --- | --- |
| `POST` | `/api/workspaces/{workspaceId}/service-principals` | Creates a principal and returns its credential once |
| `GET` | `/api/workspaces/{workspaceId}/service-principals` | Lists metadata; never returns a credential or hash |
| `POST` | `/api/workspaces/{workspaceId}/service-principals/{principalId}/rotate` | Replaces the credential and returns the new value once |
| `DELETE` | `/api/workspaces/{workspaceId}/service-principals/{principalId}` | Revokes the principal immediately |

Create body:

```json
{
  "name": "approval-sync",
  "scopes": ["projections:read", "approvals:read"]
}
```

The `credential` field appears only in successful create and rotate responses.
Store it in the integration's secret manager; it cannot be recovered later.

## Authentication and scopes

Use `Authorization: Bearer msp_...`. A principal can call
`GET /api/service-principal/identity` to inspect its actor ID, workspace,
accountable owner, and fixed scopes. This endpoint grants no business access.

Supported scopes are `projections:create`, `projections:read`,
`projections:update`, `approvals:read`, and `approvals:decide`. Every integration
endpoint opts into one exact scope. A missing, unknown, or merely
prefix-matching scope returns `403 Forbidden`.

## Immutable external issues

`POST /api/integration/issues` atomically creates or returns the issue bound to
an external task. It requires `projections:create`:

```json
{
  "external_issue_ref": {
    "provider": "overtime",
    "instance_id": "primary",
    "external_id": "task-1842",
    "external_url": "https://overtime.example/tasks/task-1842"
  },
  "issue": {
    "title": "Implement invoice export",
    "description": "Source: Overtime task task-1842",
    "status": "todo",
    "priority": "medium",
    "assignee_type": "agent",
    "assignee_id": "00000000-0000-0000-0000-000000000000",
    "project_id": "00000000-0000-0000-0000-000000000000"
  }
}
```

The immutable identity is `(workspace, provider, instance_id, external_id)`.
The first request returns `201` and `created: true`; an identical retry returns
`200`, `created: false`, and the original issue. A different normalized issue
payload or backlink for the same key returns `409` and does not mutate the
existing issue. Terminal issues are returned as-is and are never reopened.

Resolve a reference with `projections:read`:

```text
GET /api/integration/issues?provider=overtime&instance_id=primary&external_id=task-1842
```

Responses contain `external_issue_ref`, the current `issue`, a stable
`issue_path`, and `issue_url` when the deployment has configured
`MULTICA_APP_URL` or `FRONTEND_ORIGIN`. The backlink must be HTTP(S) and cannot
contain user credentials or query parameters. Neither credentials nor the
internal payload digest are returned.

The CLI exposes the same contract. Supply the service-principal credential via
`MULTICA_TOKEN` and its workspace via `MULTICA_WORKSPACE_ID`:

```bash
multica issue external create \
  --provider overtime --instance-id primary --external-id task-1842 \
  --external-url https://overtime.example/tasks/task-1842 \
  --title "Implement invoice export" --assignee-type agent \
  --assignee-id 00000000-0000-0000-0000-000000000000

multica issue external get overtime primary task-1842
```

Service-principal credentials cannot call the member/agent API, manage their own
scopes, become assignees or human approvers, start agents, access shell or
repository/deployment secrets, or impersonate their owner. A projection may
record a workspace-invocable agent or squad as assignee, but creation does not
enqueue a run; private and member-targeted invoke gates remain closed.
